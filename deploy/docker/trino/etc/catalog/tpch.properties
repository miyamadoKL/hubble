# Demo data source for the docker-compose stack. The tpch connector generates
# deterministic benchmark data on the fly (schemas: tiny, sf1, sf100, ...), so
# Hubble has something to browse and query out of the box with no external DB.
connector.name=tpch
